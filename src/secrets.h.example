#pragma once

// Copy this file to secrets.h and fill in your Wi-Fi credentials.
// secrets.h is gitignored so real credentials stay out of the repo.

constexpr const char* WIFI_SSID = "YOUR_SSID";
constexpr const char* WIFI_PASS = "YOUR_PASSWORD";
